#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    float a,b,c;
    cin >> a >> b ;
    c=1/(1/a+1/b);
    cout <<setiosflags(ios::fixed)<<setprecision(2)<< c << endl;
    return 0;
}
